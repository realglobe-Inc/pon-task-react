/**
 * Test case for generateScopedName.
 * Runs with mocha.
 */
'use strict'

const generateScopedName = require('../lib/helpers/generateScopedName.js')
const assert = require('assert')
const co = require('co')

describe('generate-scoped-name', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Generate scoped name', async () => {

  })
})

/* global describe, before, after, it */
