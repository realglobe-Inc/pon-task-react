/**
 * Test case for processCss.
 * Runs with mocha.
 */
'use strict'

const processCss = require('../lib/helpers/processCss.js')
const assert = require('assert')
const co = require('co')

describe('process-css', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Process css', () => co(function * () {

  }))
})

/* global describe, before, after, it */
